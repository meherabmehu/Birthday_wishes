name = "cute-billi-ai-wishes"
main = "src/index.js"
# Keep this at a date Cloudflare has already reached in every timezone.
compatibility_date = "2026-08-01"

# Do NOT put GROQ_API_KEY here. Add it as a Cloudflare Worker secret.
# After GitHub Pages is live, set ALLOWED_ORIGIN as a Worker secret too,
# for example: https://meherabmehu.github.io
